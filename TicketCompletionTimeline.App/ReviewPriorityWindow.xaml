<Window x:Class="TicketCompletionTimeline.App.ReviewPriorityWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Operational review priority" Width="620" Height="520" WindowStartupLocation="CenterOwner" Background="#F5F8FC">
    <Grid Margin="24">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <TextBlock Text="Operational review priority" FontSize="23" FontWeight="SemiBold" Foreground="#0B4F88" />
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,8,0,16">
            <Border Background="{Binding BandBrush}" CornerRadius="5" Padding="12,6"><TextBlock Text="{Binding BandText}" Foreground="White" FontWeight="SemiBold" /></Border>
            <TextBlock Text="  Observable patterns for supervisor review — not a conclusion about intent or performance." Foreground="#637A90" VerticalAlignment="Center" TextWrapping="Wrap" Margin="8,0,0,0" />
        </StackPanel>
        <Border Grid.Row="2" Background="White" BorderBrush="#D8E5F0" BorderThickness="1" CornerRadius="8" Padding="14">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Evidence}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#F7FAFD" BorderBrush="#D8E5F0" BorderThickness="1" CornerRadius="5" Padding="10" Margin="0,0,0,8">
                                <Grid>
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="190"/><ColumnDefinition Width="110"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding Title}" FontWeight="SemiBold" Foreground="#31516D" TextWrapping="Wrap" />
                                    <TextBlock Grid.Column="1" Text="{Binding PointsText}" Foreground="#8A5A00" />
                                    <TextBlock Grid.Column="2" Text="{Binding Detail}" Foreground="#637A90" TextWrapping="Wrap" />
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>
        <Button Grid.Row="3" Content="Close" MinWidth="90" HorizontalAlignment="Right" Margin="0,16,0,0" Background="#E5F0FB" Foreground="#164A78" Click="Close_Click" />
    </Grid>
</Window>
